! function () {
    const e = document.documentElement;
    if (e.classList.remove("no-js"), e.classList.add("js"), document.body.classList.contains("has-animations")) {
        const e = window.sr = ScrollReveal();
        e.reveal(".hero-title, .hero-paragraph, .hero-cta", {
            duration: 1e3,
            distance: "40px",
            easing: "cubic-bezier(0.5, -0.01, 0, 1.005)",
            origin: "left",
            interval: 150
        }), e.reveal(".hero-illustration", {
            duration: 1e3,
            distance: "40px",
            easing: "cubic-bezier(0.5, -0.01, 0, 1.005)",
            origin: "right",
            interval: 150
        }), e.reveal(".feature", {
            duration: 1e3,
            distance: "40px",
            easing: "cubic-bezier(0.5, -0.01, 0, 1.005)",
            interval: 100,
            origin: "bottom",
            scale: .9,
            viewFactor: .5
        }), document.querySelectorAll(".pricing-table").forEach(i => {
            const t = [].slice.call(i.querySelectorAll(".pricing-table-header")),
                a = [].slice.call(i.querySelectorAll(".pricing-table-features li")),
                c = [].slice.call(i.querySelectorAll(".pricing-table-cta")),
                r = t.concat(a).concat(c);
            e.reveal(r, {
                duration: 600,
                distance: "20px",
                easing: "cubic-bezier(0.5, -0.01, 0, 1.005)",
                interval: 100,
                origin: "bottom",
                viewFactor: .5
            })
        })
    }
}();

document.querySelectorAll('a[href^="#"]').forEach(link => {
    link.addEventListener('click', function (e) {
        var anchor = this.getAttribute('href');
        var targetElement = document.querySelector(anchor);

        if (targetElement) {
            e.preventDefault(); // 阻止默认锚点跳转行为
            var targetPosition = targetElement.getBoundingClientRect().top + window.scrollY;

            window.scrollTo({
                top: targetPosition,
                behavior: 'smooth' // 平滑滚动到目标位置
            });
        }
    });
});

function disableSubmitButton() {
    var submitButton = document.getElementById('submit-button');
    submitButton.disabled = true;
    submitButton.classList.remove('button-primary', 'button-shadow');
    submitButton.classList.add('button-disabled');
    submitButton.textContent = '已提交！';

    // // 显示提交成功的提示信息
    // var message = document.createElement('div');
    // message.className = 'message-success';
    // message.textContent = '已成功提交！';
    // submitButton.parentNode.appendChild(message);
}

document.getElementById('myForm').addEventListener('submit', function (event) {
    event.preventDefault();

    var formData = new FormData(this);

    fetch('https://script.google.com/macros/s/AKfycbw65Zqc0IoCZvignzZmdTQxWNRd1dB81UdqWhX4j6GGwVbbpKZ05JvudXnX_cmT8VK5/exec', {
        method: 'POST',
        body: formData
    })
        .then(response => response.json())
        .then(data => {
            if (data.status === 'success') {
                disableSubmitButton();
            }
        })
        .catch(function (error) {
            console.error('Error:', error);
            disableSubmitButton();
        });
});
